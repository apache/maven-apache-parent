# override the default maven project name locally only for this project
# we can not change the project name in the pom.xml file because it is a parent project
# and changing the name will break all child projects
-Datr.project=maven-apache-parent
